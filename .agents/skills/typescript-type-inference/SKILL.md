---
name: typescript-type-inference
description: Apply project type-inference rules when writing or editing TypeScript code.
---

Use TypeScript type inference in new code. Do not add explicit type annotations when TypeScript can infer the same type from a value, return expression, callback context, or generic usage. Add explicit types only when inference is unavailable or when the type defines an intentional contract.
